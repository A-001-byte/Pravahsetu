"""Application settings, loaded from environment variables (see
.env.example at the repo root).

TODO (Module 6): define a Pydantic BaseSettings class covering
DATABASE_URL, CORS origins, and the other variables in .env.example.
"""
