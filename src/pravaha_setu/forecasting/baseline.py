"""Baseline short-term reservoir inflow forecasting using scikit-learn
(Random Forest / Gradient Boosting).

This baseline is kept as a permanent comparison point against the LSTM
model (lstm_model.py), not a throwaway prototype.

TODO (Module 3): implement train/predict functions and a shared evaluation
harness so baseline and LSTM results are directly comparable.
"""
